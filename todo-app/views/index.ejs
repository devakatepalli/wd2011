<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My Todo App</title>
    <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
    <%- include("header") %>

    <h1>This is my Todo Application</h1>

    <form action="/todos" method="POST">
        <input type="text" name="title" placeholder="Enter todo title" required>
        <input type="date" name="dueDate" required>
        <button type="submit">Add</button>
    </form>

    <h5>Overdue (<span id="count-overdue"><%= overdueTodos.length %></span>)</h5>
    <%- include("todos", { todos: overdueTodos }) %>

    <h5>Due Today (<span id="count-due-today"><%= dueTodayTodos.length %></span>)</h5>
    <%- include("todos", { todos: dueTodayTodos }) %>

    <h5>Due Later (<span id="count-due-later"><%= dueLaterTodos.length %></span>)</h5>
    <%- include("todos", { todos: dueLaterTodos }) %>

    <%- include("footer") %>
</body>
</html>
